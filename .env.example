# 浏览器访问端口
APP_PORT=8080

# 默认 SQLite。未来可改为 postgresql+psycopg://... 或 mysql+pymysql://...
# 改用其他数据库时，还需在 backend/requirements.txt 加入对应驱动。
DATABASE_URL=sqlite:////data/vocab.db
CORS_ORIGINS=http://localhost:8080

