# 直接部署配置（不使用 Docker）
APP_HOST=0.0.0.0
APP_PORT=8080

# 留空时启动脚本会使用项目目录下的 data/vocab.db 与 frontend/dist。
# DATABASE_URL=sqlite:////absolute/path/to/data/vocab.db
# FRONTEND_DIST=/absolute/path/to/frontend/dist

CORS_ORIGINS=http://localhost:8080

# 可选：自定义虚拟环境位置
# VENV_PATH=/absolute/path/to/.venv

